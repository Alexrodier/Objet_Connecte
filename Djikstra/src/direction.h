#include <iostream>
using namespace std;

class Direction{
  public:

    Direction(char from, char by, char to); //Constructeur
    char from;
    char by;
    char to;
};

Direction::Direction(char froms, char bys, char to){ //Constructeur
  from = from;
  by = bys;
  to = to;
}